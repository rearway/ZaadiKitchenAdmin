# Set VITE_API_BASE_URL in your deployment environment (Amplify, etc.)
# Local dev: leave unset — falls back to /api/v1 which Vite proxies automatically.
# Example: VITE_API_BASE_URL=https://devapi.platiokitchen.com/api/v1
