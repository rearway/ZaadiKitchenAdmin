# Local dev — leave empty so Vite proxy handles /api/* requests.
# In Amplify (or any deployment), set VITE_API_BASE_URL to the API origin, e.g.:
#   VITE_API_BASE_URL=https://devapi.zaadikitchen.com
VITE_API_BASE_URL=
